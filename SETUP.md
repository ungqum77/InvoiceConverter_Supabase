# 작업 순서 (Step by Step)

새 Supabase 프로젝트로 송장.com을 처음부터 세팅하는 순서입니다.
**위에서부터 하나씩** 진행하시고, 각 단계의 "확인" 항목이 통과하면 다음으로 넘어가세요.

막히면 그 단계 번호와 에러 메시지를 저에게 그대로 주시면 됩니다.

---

## 시작 전에 — 지금 상태

| 항목 | 상태 |
|---|---|
| 코드 | `cleanup-vat-suppliers` 브랜치에 최신본 적용됨 |
| `main` 브랜치 | 손대지 않음 (Vercel 배포본 그대로 살아있음) |
| Supabase | **새로 만들 예정** ← 지금부터 |
| 기존 DB (`klxiareqtqvcobdgwswq`) | 그대로 있음. 나중에 지우셔도 되고 두셔도 됩니다 |

> ⚠️ **새 프로젝트를 만들면 기존 데이터(제품·송장양식·회원)는 따라오지 않습니다.**
> 지금까지 등록한 제품이 많고 그걸 살리고 싶으시면, 이 문서 대신
> 기존 프로젝트에 `supabase/migration.sql` 만 실행하는 방법이 있습니다. 말씀해 주세요.

---

## STEP 1. Supabase 프로젝트 만들기

1. https://supabase.com/dashboard 접속 → **New project**
2. 입력값
   - **Name**: `songjang` (아무거나 괜찮습니다)
   - **Database Password**: 강한 비밀번호 생성 후 **어딘가에 저장** (다시 못 봅니다)
   - **Region**: `Northeast Asia (Seoul)` — 한국 사용자면 이게 제일 빠릅니다
3. 생성까지 1~2분 기다립니다

**확인** — 프로젝트 대시보드가 열리면 완료

---

## STEP 2. 테이블 만들기

1. 왼쪽 메뉴 **SQL Editor** → **New query**
2. VS Code에서 `supabase/full-schema.sql` 열기 → `Ctrl+A` → `Ctrl+C`
3. SQL Editor에 붙여넣고 우측 하단 **Run** (또는 `Ctrl+Enter`)

**확인** — 하단에 `Success. No rows returned` 가 나오면 성공.
왼쪽 **Table Editor** 에 테이블 11개가 보여야 합니다.

```
tiers  profiles  suppliers  invoice_templates  products
sales_records  app_settings  activity_logs  analytics_events
blog_posts  user_guides
```

> 에러가 나면 메시지 전체를 저에게 주세요. 대부분 한 줄 고치면 됩니다.

---

## STEP 3. 접속 정보 가져오기

1. 왼쪽 메뉴 맨 아래 **Project Settings** → **API**
2. 두 개를 복사해 둡니다
   - **Project URL** — `https://xxxxx.supabase.co`
   - **anon / public key** — `eyJ...` 로 시작하는 긴 문자열

> `service_role` 키는 **절대 쓰지 마세요.** 그건 서버 전용이고 유출되면 DB 전체가 열립니다.
> 우리가 쓸 건 `anon` 키입니다. 이건 공개되어도 되는 키이고, STEP 2에서 RLS를 켰기 때문에
> 이 키만으로는 남의 데이터를 못 봅니다.

---

## STEP 4. 앱에 새 접속 정보 연결 ✅ (완료됨)

**환경변수 방식(방법 A)으로 이미 바꿔놨습니다.** `services/supabase.ts` 에 있던
옛날 프로젝트 주소·키는 코드에서 지웠고, 이제 아래 두 값을 읽어옵니다.

```
VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY
```

대표님이 하실 일은 **값 채워넣기 하나**입니다.

1. VS Code에서 프로젝트 루트에 `.env.local` 파일을 새로 만듭니다
   (`.env.local.example` 파일을 복사해서 이름만 바꾸셔도 됩니다)
2. STEP 3에서 복사한 값을 넣습니다

```
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
```

> `=` 앞뒤에 공백 넣지 마시고, 값에 따옴표도 붙이지 마세요.
> `.env.local` 은 `.gitignore` 에 들어있어서 GitHub에 올라가지 않습니다. 안전합니다.

3. 이미 `npm run dev` 가 돌고 있었다면 **껐다 다시 켜야** 반영됩니다 (`Ctrl+C` → `npm run dev`)

**확인** — 앱을 열었을 때 "환경변수가 설정되지 않았습니다" 안내가 안 뜨면 성공.
값이 잘못되면 화면에 무엇이 잘못됐는지 한글로 알려줍니다.

> 배포할 때는 같은 값 두 개를 Vercel → Settings → Environment Variables 에도
> 등록해야 합니다. STEP 11 참고.

---

## STEP 5. 실행하고 회원가입

VS Code 터미널(`Ctrl + ~`)에서:

```powershell
npm run dev
```

1. 브라우저에서 http://localhost:3000 열기
2. **회원가입** — 실제 쓰실 이메일로 가입하세요
3. Supabase 대시보드 → **Table Editor** → `profiles` 테이블에 내 계정이 한 줄 생겼는지 확인

**확인** — `profiles` 에 행이 생겼으면 트리거가 정상 작동한 것입니다.
행이 없으면 저에게 말씀해 주세요.

---

## STEP 6. 나를 관리자로 승격

가입한 계정은 기본 '무료 회원'이라 제품 2개까지만 등록됩니다. 관리자로 올립니다.

Supabase → **SQL Editor** → 새 쿼리:

```sql
update public.profiles
   set role = 'super_admin', tier_id = 'admin'
 where email = 'ungqum77@gmail.com';
```

이메일이 다르면 본인 것으로 바꾸세요.

**확인** — 앱에서 새로고침 → 제품 관리 화면 우측 상단에 **관리자** 배지가 뜨면 성공

---

## STEP 7. 송장 양식 등록

발주처가 요구하는 엑셀 양식을 그대로 올리는 단계입니다.

1. 제품 관리 → **송장 양식** 탭 → **엑셀 업로드**
2. 올릴 엑셀의 구조
   - **1행** = 내 주문서에서 가져올 컬럼 이름 (매칭용)
   - **2행** = 발주처에게 보낼 때 찍힐 이름 (선택. 없으면 1행을 그대로 씀)
3. **파일 이름이 양식 이름**이 됩니다. `한일식품.xlsx` → 양식명 `한일식품`

**확인** — 카드가 생기고 컬럼 개수가 맞게 표시되면 완료

---

## STEP 8. 발주처 등록 ⭐ 새 기능

1. 제품 관리 → **발주처** 탭 → **발주처 등록**
2. 발주처마다 입력
   - 발주처명 (필수)
   - 담당자 / 연락처 / 결제조건
   - **매입가 기준** ← 중요. 아래 설명 참고

**"매입가 기준" 이 뭔가**

제품에 등록할 매입가가 부가세를 포함한 금액인지 아닌지입니다.

| 설정 | 매입가 10,000원일 때 | 실제 지급액 |
|---|---|---|
| 부가세 포함가 (기본) | 공급가액 9,091 + 부가세 909 | **10,000원** |
| 부가세 별도 | 공급가액 10,000 + 부가세 1,000 | **11,000원** |

발주처가 세금계산서를 어떻게 끊는지에 맞춰 고르시면 됩니다. 헷갈리면 기본값(포함) 두세요.

**확인** — 목록에 발주처가 뜨면 완료

---

## STEP 9. 제품(SKU) 등록

1. 제품 관리 → **제품 목록** 탭 → **개별 등록**
2. 입력
   - **SKU** — 주문서에 찍히는 코드. 이 값으로 대조합니다
   - **제품명** — 발주처 송장에 찍힐 이름
   - **발주처** — STEP 8에서 등록한 목록에서 선택
   - **송장 양식** — STEP 7에서 등록한 것 선택
   - **과세 구분** — 과세 / 면세
   - **매입가** — 발주처에 줄 돈. **여기가 정산의 기준입니다**
   - 판매가 / 택배비 / 기타비용 / 수수료율 — 마진 계산용 (선택)

**제품이 많으면** → **대량 등록** 버튼 → 샘플 엑셀 받아서 채운 뒤 업로드

**확인** — 목록에 뜨고 '예상마진'이 계산되면 완료

---

## STEP 10. 실제 주문서로 테스트

**바로 실전에 쓰지 마시고 먼저 테스트하세요.**

1. **송장 변환** 메뉴 → 실제 주문서 엑셀 업로드
2. 컬럼 매핑 확인 (자동으로 잡아주지만 틀릴 수 있으니 눈으로 확인)
3. **변환 시작**
4. 정산 요약 표에서 확인할 것
   - [ ] 발주처가 빠짐없이 나오는가
   - [ ] 수량이 맞는가
   - [ ] 공급가액 + 부가세 = 지급액 인가
   - [ ] 지급액이 실제로 줄 금액과 맞는가 ← **직접 계산해서 대조해보세요**
5. **ZIP 다운로드** → 압축 풀어서 발주처별 파일 열어보기
   - [ ] 제품명이 발주처가 쓰는 이름으로 바뀌었는가
   - [ ] 수량, 수취인, 주소가 제대로 들어갔는가

**확인** — 위 항목이 다 맞으면 실전 투입 가능

---

## STEP 11. 배포 (Vercel)

로컬에서 문제없이 돌아가면 올립니다.

```powershell
git add -A
git commit -m "새 Supabase 프로젝트 연결"
git push -u origin cleanup-vat-suppliers
```

1. GitHub 저장소 → **Compare & pull request** → PR 생성
2. 내용 확인 후 **Merge**
3. Vercel이 자동으로 재배포합니다

**STEP 4에서 방법 A(환경변수)를 택했다면** — Vercel → Settings → Environment Variables 에
값을 등록해야 합니다. 안 하면 배포본이 DB에 접속하지 못합니다.

**확인** — 배포된 주소에서 로그인 → 제품 목록이 보이면 완료

---

## 저에게 시킬 때

VS Code에서 이 폴더를 열어두시고, 그냥 말씀하시면 제가 파일을 직접 고칩니다.

```
"발주처 탭에 검색창 넣어줘"
"정산요약에 전월 대비 컬럼 추가해줘"
"제품 목록에서 발주처별로 필터링하고 싶어"
"이 에러 좀 봐줘: [에러 메시지 붙여넣기]"
```

**제가 못 하는 것** — 대표님 PC에서 명령어 실행. `npm`, `git` 은 직접 쳐주셔야 합니다.

---

## 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| 로그인은 되는데 데이터가 안 보임 | RLS 정책 문제. `profiles` 에 내 행이 있는지 확인 |
| "발주처 기능을 아직 쓸 수 없습니다" | STEP 2 SQL이 실행 안 됨. 다시 실행 |
| 제품 등록이 2개에서 막힘 | STEP 6 관리자 승격 안 함 |
| 변환 후 정산금이 0원 | 제품에 **매입가**를 안 넣음 |
| 발주처 파일이 안 만들어짐 | 제품에 **송장 양식**이 지정 안 됨 |
| 폴더 자동저장이 안 됨 | Chrome 또는 Edge에서만 됩니다. 파이어폭스 ✗ |

에러 메시지는 잘라내지 말고 **전체를 그대로** 주세요. 브라우저 개발자도구(`F12`) →
Console 탭에 빨간 글씨가 있으면 그것도 같이 주시면 훨씬 빠릅니다.

---

## 참고 문서

- `README.md` — 프로젝트 구조와 계산 규칙
- `supabase/full-schema.sql` — 새 프로젝트용 전체 스키마 (STEP 2에서 사용)
- `supabase/migration.sql` — 기존 프로젝트에 새 기능만 추가할 때 쓰는 스크립트
- `services/calc.ts` — 모든 금액 계산이 모여 있는 파일
